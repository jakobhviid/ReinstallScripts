# This file is YOURS to edit. The managed config lives in ~/.zshrc.image
# (auto-generated by `just zsh` from ReinstallScripts/Mac/assets/zshrc.template).
# Keep the source line below so the managed config still loads; add or
# override anywhere around it. Tools that auto-modify .zshrc (nvm, pyenv,
# bun, etc.) will not be clobbered on the next `just zsh`.
source ~/.zshrc.image
